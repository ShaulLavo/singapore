{
  "name": "@singapore-editor/textbuffer",
  "version": "0.1.0",
  "description": "Persistent treap-backed text buffer with snapshots, stable anchors, and line mapping",
  "files": ["dist", "README.md"],
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js", "default": "./dist/index.js" },
    "./debug": { "types": "./dist/debug.d.ts", "import": "./dist/debug.js", "default": "./dist/debug.js" },
    "./diagnostics": { "types": "./dist/diagnostics.d.ts", "import": "./dist/diagnostics.js", "default": "./dist/diagnostics.js" },
    "./internal/*": { "types": "./dist/*.d.ts", "import": "./dist/*.js", "default": "./dist/*.js" }
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:package": "node test/package-smoke.mjs",
    "verify": "bun run typecheck && bun run build && bun run test && bun run test:package",
    "bench": "node bench/run.mjs",
    "bench:prepare": "node bench/prepare.mjs",
    "bench:test": "vitest run --config bench/vitest.config.mjs",
    "bench:check": "bun run build && bun run bench:prepare && bun run bench:test",
    "lint": "oxlint .",
    "format": "oxfmt --write .",
    "format:check": "oxfmt --check ."
  },
  "devDependencies": {
    "oxfmt": "0.54.0",
    "oxlint": "1.69.0",
    "typescript": "~6.0.3",
    "vitest": "^4.1.11"
  },
  "packageManager": "bun@1.3.14"
}
